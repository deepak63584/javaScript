const harnum = require('./utility')
const readline = require('readline-sync')

var hormonic = readline.question("Enter the no you want to find hormonic : \n")

console.log(harnum.harmonicnumber(hormonic));

